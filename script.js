
let numA = 10;
let numB = 5;
let result = divide(numA, numB);
console.log(result);

numA = 15;
numB = 0;
result = divide(numA, numB);
console.log(result);

result = divide(numB, numA);
console.log(result);


result = divide(numB, numA);
console.log(result);


function divide(a, b) {
    if(b !== 0) {
        return a/b;
    }
    return null;
}

result = divide(numB, numA);
console.log(result);


function divide(a, b) {
    if(b !== 0) {
        return a/b;
    }
    return null;
}
